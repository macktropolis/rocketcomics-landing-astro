---
const { siteName, author, url, logo, description } = Astro.props;
---

<header id="site-header" class="header">
    <h1 class="header__title">{siteName}</h1>
    <h2 class="header__subtitle">{description}</h2>
    <img src={logo} alt={siteName} class="header__logo" id="site-logo" />
</header>